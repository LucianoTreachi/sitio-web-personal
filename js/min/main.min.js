const hamburgerButton = document.querySelector(".hamburger-button"), closeMenuButton = document.querySelector(".close-menu-button"), navbar = document.querySelector(".navbar"), navLinks = document.querySelectorAll(".nav-link"), overlay = document.querySelector(".overlay"); hamburgerButton.addEventListener("click", () => { navbar.classList.add("active"), overlay.classList.add("active"), hamburgerButton.setAttribute("aria-expanded", "true") }), closeMenuButton.addEventListener("click", () => { navbar.classList.remove("active"), overlay.classList.remove("active"), hamburgerButton.setAttribute("aria-expanded", "false") }), overlay.addEventListener("click", () => { navbar.classList.remove("active"), overlay.classList.remove("active"), hamburgerButton.setAttribute("aria-expanded", "false") }), navLinks.forEach(e => { e.addEventListener("click", () => { navbar.classList.remove("active"), overlay.classList.remove("active"), hamburgerButton.setAttribute("aria-expanded", "false") }), e.addEventListener("keydown", t => { "Enter" === t.key && e.click() }) }), window.addEventListener("scroll", () => { navbar.classList.remove("active"), overlay.classList.remove("active"), hamburgerButton.setAttribute("aria-expanded", "false") }), window.addEventListener("scroll", () => { let e = document.querySelector("header"); e.classList.toggle("active", window.scrollY > 0) }); const sections = document.querySelectorAll("section"); function setActiveLink() { let e = sections.length; for (; --e && window.scrollY + 50 < sections[e].offsetTop;); navLinks.forEach(e => e.classList.remove("active")), e > 0 && navLinks.forEach(t => { t.getAttribute("href") === "#" + sections[e].id && t.classList.add("active") }) } window.addEventListener("scroll", setActiveLink), navLinks.forEach(e => { e.addEventListener("click", t => { t.preventDefault(); let a = e.getAttribute("href").substring(1), r = document.getElementById(a); r && r.scrollIntoView({ behavior: "smooth", block: "start" }) }), e.addEventListener("keydown", t => { if ("Enter" === t.key) { t.preventDefault(); let a = e.getAttribute("href").substring(1), r = document.getElementById(a); r && r.scrollIntoView({ behavior: "smooth", block: "start" }) } }) });